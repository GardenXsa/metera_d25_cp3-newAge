diff --git a/main.js b/main.js
--- a/main.js
+++ b/main.js
@@ ipcMain.handle('save-world-state', async (event, filename, data) => {
-    const name = data.name || "Неизвестный мир";
-    const era = data.era || "rebirth";
-    delete data.name;
-    delete data.era;
-    const orderedData = { name, era, ...data };
-    fs.writeFileSync(path.join(WORLDS_DIR, filename), JSON.stringify(orderedData));
+    const { name = "Неизвестный мир", era = "rebirth", ...rest } = data || {};
+    const orderedData = { name, era, ...rest };
+    fs.writeFileSync(path.join(WORLDS_DIR, filename), JSON.stringify(orderedData));
@@ const server = http.createServer((req, res) => {
-    const urlObj = new URL(req.url, getServerOrigin());
-    const token = urlObj.searchParams.get('token');
-    if (token !== HTTP_SESSION_TOKEN) {
+    const urlObj = new URL(req.url, getServerOrigin());
+    const token = urlObj.searchParams.get('token') ||
+                  req.headers['x-session-token'] ||
+                  req.headers['X-Session-Token'];
+    if (token !== HTTP_SESSION_TOKEN) {
@@ function createWindow() {
-    win.webContents.setWindowOpenHandler(({ url }) => {
-        // Allow external links using configured protocols.
-        for (const protocol of EXTERNAL_LINK_PROTOCOLS) {
-            if (url.startsWith(protocol)) {
-                shell.openExternal(url);
-                return { action: 'deny' };
-            }
-        }
-        return { action: 'allow' };
-    });
+    win.webContents.setWindowOpenHandler(({ url }) => {
+        const origin = getServerOrigin();
+        if (url.startsWith(origin)) {
+            return { action: 'allow' };
+        }
+        shell.openExternal(url);
+        return { action: 'deny' };
+    });
+    win.webContents.on('will-navigate', (event, url) => {
+        const origin = getServerOrigin();
+        if (!url.startsWith(origin)) {
+            event.preventDefault();
+            shell.openExternal(url);
+        }
+    });
@@ function createWindow() {
-    win.loadURL(`${getServerOrigin()}?token=${HTTP_SESSION_TOKEN}`);
+    win.loadURL(getServerOrigin());

diff --git a/js/mods/ModLoader.js b/js/mods/ModLoader.js
--- a/js/mods/ModLoader.js
+++ b/js/mods/ModLoader.js
@@ const SANDBOX_BLOCKED_GLOBALS = new Set([
-    'Navigator', 'Location', 'History',
+    'Navigator', 'Location', 'History',
+    'navigator', 'location', 'history',
@@
 const WINDOW_BLOCKED_PROPS = new Set([
@@
     'crypto',
 ]);
+
+// Allowed game globals that may be exposed via the safe window proxy.
+const WINDOW_ALLOWED_PASSTHROUGH = new Set([
+    'player',
+    't',
+    'World',
+    'updateCharacterSheet',
+    'damagePlayerHP',
+]);
@@ function createSafeWindowProxy(realWindow, modId, safeModAPI) {
-            // Block dangerous properties
-            if (WINDOW_BLOCKED_PROPS.has(prop)) {
-                 console.warn(`[Sandbox:${modId}] Блокировка доступа window.${String(prop)}`);
-                 return undefined;
-            }
-
-            // Pass-through to real window for non-blocked properties
-            const value = realWindow[prop];
-            // If it's a function, bind it to the real window to preserve 'this'
-            if (typeof value === 'function') {
-                return value.bind(realWindow);
-            }
-            return value;
+            // Block dangerous properties
+            if (WINDOW_BLOCKED_PROPS.has(prop)) {
+                console.warn(`[Sandbox:${modId}] Блокировка доступа window.${String(prop)}`);
+                return undefined;
+            }
+            // Only allow explicitly whitelisted properties
+            if (!WINDOW_ALLOWED_PASSTHROUGH.has(prop)) {
+                console.warn(`[Sandbox:${modId}] Запрос window.${String(prop)} не разрешён в песочнице`);
+                return undefined;
+            }
+            const value = realWindow[prop];
+            if (typeof value === 'function') {
+                return value.bind(realWindow);
+            }
+            return value;
@@ function createModSandbox(modAPI, modId, modMeta) {
-            // 3. Pass-through: check if it's a game global on window.
-            //    Dangerous window properties are already filtered by step 2
-            //    (fetch, eval, etc. are in both SANDBOX_BLOCKED_GLOBALS
-            //     and WINDOW_BLOCKED_PROPS).
-            //    This allows `player`, `t`, `World`, `updateCharacterSheet`,
-            //    `damagePlayerHP`, etc. to work as bare identifiers.
-            if (prop in window) {
-                return window[prop];
-            }
+            // 3. Pass-through: allow only whitelisted game globals from window.
+            if (WINDOW_ALLOWED_PASSTHROUGH.has(prop) && prop in window) {
+                return window[prop];
+            }
@@ function createModSandbox(modAPI, modId, modMeta) {
-            // For game globals, pass through to window
-            if (prop in window) {
-                window[prop] = value;
-                return true;
-            }
+            // For game globals, pass through to window (only if allowed)
+            if (WINDOW_ALLOWED_PASSTHROUGH.has(prop) && prop in window) {
+                window[prop] = value;
+                return true;
+            }
@@ function createModSandbox(modAPI, modId, modMeta) {
-            // Pass through to window for game globals
-            if (prop in window) {
-                delete window[prop];
-                return true;
-            }
+            // Pass through to window for game globals (only if allowed)
+            if (WINDOW_ALLOWED_PASSTHROUGH.has(prop) && prop in window) {
+                delete window[prop];
+                return true;
+            }